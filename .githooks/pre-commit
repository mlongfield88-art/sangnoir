#!/usr/bin/env bash
# gitleaks pre-commit hook (SEC-2026-016 mitigation, AUD-2026-004)
set -e
if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks not installed. Run: brew install gitleaks"
  exit 1
fi
gitleaks protect --staged --redact --no-banner --verbose
